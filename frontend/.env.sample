# Frontend env for local development (copy to .env and fill values)
VITE_AZURE_AD_CLIENT_ID=
VITE_AZURE_AD_TENANT_ID=
VITE_AZURE_AD_BACKEND_CLIENT_ID=
# For production builds, set to your API custom domain (example below)
VITE_API_BASE=https://api.danstcg.com

# Note: This comment exists only to trigger CI/CD workflows; no functional change.
